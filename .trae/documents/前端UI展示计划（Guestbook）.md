## 展示目标

* 全面展示主要页面与核心功能模块的实际界面与交互

* 覆盖页面布局、设计风格、交互与动画、响应式表现、组件视觉

* 演示完整用户流程与关键错误/边界状态

## 执行方式

* 在开发环境运行完整应用入口（`src/App-new.tsx:6–12`，挂载 `MainApp-new`），确保路由与全局 `Toaster` 生效（`src/App-new.tsx:10–35`）

* 启动本地预览并提供实时访问链接，然后录制短视频 + 截图集

* 若数据服务未配置，先通过“添加记录”页建立少量演示数据；无数据页展示空态与加载态亦纳入演示

## 演示流程

* 登录/注册

  * 登录表单交互与错误提示（必填校验与错误 toast：`src/pages/Login.tsx:18–21, 28–30`）

  * 成功后跳转首页（`Login.tsx:26–28`）与注册页访问（`Login.tsx:111–118`）

* 首页/布局

  * 使用主布局承载子路由与底部导航（`src/components/Layout.tsx:18–27, 33–41, 43–45`）

  * 展示底部导航的激活态/悬停过渡与整体配色

* 礼簿列表

  * 搜索与筛选交互、统计卡片视觉（`src/pages/Records.tsx:60–84, 86–118`）

  * 列表卡片视觉与点击跳转（`Records.tsx:149–179`）

  * 空态与提示引导（`Records.tsx:124–141`）与加载指示（`Records.tsx:34–40`）

* 新增记录

  * 表单结构与常用事件、支付方式（`src/pages/RecordAdd.tsx:85–115, 192–205`）

  * 必填校验与错误 toast（`RecordAdd.tsx:25–33`），成功后反馈与跳转（`RecordAdd.tsx:48–50`）

* 联系人与我的/设置

  * 联系人列表与空态、个人页菜单与退出登录、设置的数据概览（文件位于 `src/pages/*`）

* 数据导出/导入

  * 导出入口与空数据提示（`src/pages/Records.tsx:48–55` 进入导出；导出页位于 `src/pages/Export.tsx`）

  * 导入预览与错误状态（`src/pages/Import.tsx`）

* 路由保护与加载态

  * 受保护/公开路由切换与加载指示（`src/MainApp-new.tsx:17–30, 36–45`）

* 响应式与动画

  * 移动/平板/桌面宽度下的布局自适应（Tailwind 栅格与工具类：`src/index.css:1–3` + 页面类）

  * 过渡与加载动画（如 `animate-spin`、`transition-colors`：`MainApp-new.tsx:23–25`）

## 输出交付

* 实时预览链接（开发环境）

* 10–15 张关键页面与交互的高清截图（包含空态/错误态/加载态）

* 1 段 2–3 分钟屏幕录制视频，演示完整工作流：登录→首页→礼簿→筛选→新增→导出/导入→联系人→我的/设置

* 文档化说明：版本信息、技术栈、已知问题与待优化项

## 版本与技术栈

* UI 版本：`.env:7` → `VITE_APP_VERSION=1.0.0`

* 框架与路由：React 18 + Vite + `react-router-dom`（完整路由树：`src/MainApp-new.tsx:52–83`）

* 设计系统/组件库：Tailwind CSS（`src/index.css:1–3`）、`lucide-react` 图标、`react-hot-toast` 通知

## 已知问题与待优化

* 默认入口仍指向简化版应用；完整演示需使用 `App-new`/`MainApp-new` 入口

* 底部导航使用 `<a>` 导航（`Layout.tsx:33–41`），建议换用 `NavLink` 保留 SPA 行为与激活态同步

* 响应式断点使用较少，桌面端布局可进一步优化

* 缺少专用 404 页面（当前 `*` 重定向到 `/`：`MainApp-new.tsx:81–83`）

* 暗色模式 Hook 已有（`src/hooks/useTheme.ts:14–18, 20–28`），但界面缺少显式切换入口

* 加载时骨架屏尚未提供；可补充提升感知性能

## 下一步

* 切换完整入口并启动预览，按上述流程录制视频和截图；如需连接测试数据源，我将使用受控的测试 Supabase 项目或仅展示前端空态与本地新增流。

